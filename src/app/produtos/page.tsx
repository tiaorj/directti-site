import type { Metadata } from "next"; import { Container, ProductCard, CTA } from "@/components/ui"; import { PageHero } from "@/components/content"; import { site } from "@/config/site";
export const metadata:Metadata={title:"Produtos",description:"Produtos e projetos da DIRECT TI."}; export default function Produtos(){return <><PageHero eyebrow="Produtos" title="Produtos e projetos em evolução" text="Soluções desenvolvidas e mantidas com atenção à clareza operacional, à experiência de uso e à evolução contínua."/><section className="py-16 sm:py-24"><Container><div className="grid gap-5 lg:grid-cols-3">{site.products.map(p=><ProductCard product={p} key={p.name}/>)}</div></Container></section><CTA/></>}
