"use client";
import { useState } from "react";
import { site } from "@/config/site";

export function ContactForm() { const [mode, setMode] = useState<"email" | "whatsapp">("email"); function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const message = `Nome: ${data.get("name")}\nEmpresa: ${data.get("company")}\nE-mail: ${data.get("email")}\nInteresse: ${data.get("interest")}\n\n${data.get("message")}`; const subject = encodeURIComponent(`Contato pelo site: ${data.get("interest")}`); window.location.href = mode === "email" ? `mailto:${site.email}?subject=${subject}&body=${encodeURIComponent(message)}` : `https://wa.me/${site.whatsapp}?text=${encodeURIComponent(message)}`; } return <form onSubmit={submit} className="contact-form"><div className="contact-form-grid"><label>Nome<span>*</span><input required name="name" className="field" autoComplete="name" /></label><label>Empresa<input name="company" className="field" autoComplete="organization" /></label></div><label>E-mail<span>*</span><input required type="email" name="email" className="field" autoComplete="email" /></label><label>Assunto / interesse<select name="interest" className="field" defaultValue="Modernização"><option>Modernização</option><option>Desenvolvimento</option><option>Sustentação</option><option>Integrações</option><option>Dados e BI</option><option>IA e automação</option><option>Outro</option></select></label><label>Como podemos ajudar?<span>*</span><textarea required name="message" rows={5} className="field" /></label><div className="contact-form-actions"><button className="btn btn-primary" type="submit">Enviar por {mode === "email" ? "e-mail" : "WhatsApp"} <span aria-hidden="true">→</span></button><button className="btn btn-secondary" type="button" onClick={() => setMode(mode === "email" ? "whatsapp" : "email")}>Usar {mode === "email" ? "WhatsApp" : "e-mail"}</button></div><p>Ao enviar, será aberto seu aplicativo de e-mail ou WhatsApp. Não armazenamos os dados deste formulário.</p></form>; }
