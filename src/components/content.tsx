import Link from "next/link";
import { Container } from "@/components/ui";
import { site } from "@/config/site";

export function Breadcrumb({ items }: { items: { label: string; href?: string }[] }) { return <nav aria-label="Breadcrumb" className="mb-8 text-sm text-slate-500"><ol className="flex flex-wrap gap-2">{items.map((item, i) => <li className="flex items-center gap-2" key={item.label}>{i > 0 && <span>/</span>}{item.href ? <Link className="hover:text-blue-700" href={item.href}>{item.label}</Link> : <span className="text-slate-700">{item.label}</span>}</li>)}</ol></nav>; }
export function PageHero({ eyebrow, title, text }: { eyebrow: string; title: string; text: string }) { return <section className="bg-slate-50 py-16 sm:py-24"><Container><Breadcrumb items={[{ label: "Início", href: "/" }, { label: title }]}/><p className="eyebrow">{eyebrow}</p><h1 className="mt-3 max-w-4xl text-4xl font-semibold tracking-tight text-slate-950 sm:text-5xl">{title}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-600">{text}</p></Container></section>; }
export function StructuredData() { const data = [{ "@context": "https://schema.org", "@type": "Organization", name: site.name, url: site.domain, email: site.email, sameAs: [site.linkedin, site.github] }, { "@context": "https://schema.org", "@type": "ProfessionalService", name: site.name, description: site.description, url: site.domain, areaServed: "BR" }]; return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }} />; }
